#include <stdio.h>

void increment(char *arr) {
	switch
}

int main() {
	int N = 0;
	printf("Enter the length of your number: ");
	scanf("%d", &N)
	char number[N];
	printf("Enter an integer: ");
	scanf("%s", number);


	//printf("%s", number);

	return 0;
}
